# .zshrc.d/pipx-path.zsh
# Modularized: Add pipx user base binary directory to PATH.
# Originally line 156 from .zshrc.
# Kept separate for clarity and future compatibility with other Python tools.

export PATH="$PATH:/Users/sergey/.local/bin"